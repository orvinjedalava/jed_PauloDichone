[project]
name = "anthropic-api-course"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "anthropic>=0.125.0",
    "python-dotenv>=1.2.1",
]

[project.scripts]
myapp = "anthropic_api_course:main"

[build-system]
requires = ["uv_build>=0.12.13,<0.13.0"]
build-backend = "uv_build"
